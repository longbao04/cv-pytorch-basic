# PyTorch MNIST CNN 图像分类实验报告

## 1. 项目背景

本项目使用 PyTorch 在 MNIST 手写数字数据集上训练卷积神经网络（CNN），实现数字 0～9 的图像分类。项目从基础训练与测试开始，逐步完成模型保存、加载预测、预测可视化、错误样本分析、混淆矩阵、训练曲线绘制和多组实验比较，形成了一套完整的图像分类学习流程。

本报告依据已有代码、[实验记录](experiment_notes.md)和结果 CSV 整理，不涉及重新训练。

## 2. 项目目标

- 理解图像分类的基本流程：数据准备、模型构建、训练、测试和预测。
- 理解 CNN 的输入、卷积特征提取、池化和全连接分类。
- 掌握训练、测试、保存模型以及加载模型预测的方法。
- 学会结合 accuracy、loss、错误样本和混淆矩阵分析模型。
- 学会比较 epoch、模型结构、数据增强、learning rate、batch size 和 optimizer，并通过多 seed 重复实验评估结果。

## 3. 数据集

MNIST 是手写数字图像数据集，每张图片为 **28×28 的单通道灰度图**，共 **10 个类别，分别对应数字 0～9**。训练集包含 **60000 张**图片，测试集包含 **10000 张**图片。

本项目通过 `torchvision.datasets.MNIST` 加载数据。图片经过 `ToTensor` 和 `Normalize((0.5,), (0.5,))` 预处理，再由 DataLoader 按批次提供给模型。训练集用于更新参数，测试集用于统计分类准确率。

## 4. 方法

### 模型结构

- **SimpleCNN**：输入形状为 `[batch_size, 1, 28, 28]`，使用两层 3×3 卷积，通道数依次为 16、32，每层卷积后使用 ReLU 和最大池化。空间尺寸从 28×28 缩小到 14×14，再到 7×7；展平后通过 `32×7×7 → 64 → 10` 的全连接层输出分类分数。
- **DeeperCNN**：每次池化前使用两层卷积，共四层 3×3 卷积，通道数依次为 16、16、32、32。池化后的尺寸及全连接分类部分与 SimpleCNN 相同，用于比较增加卷积层的效果。

### 损失函数、优化器与设备

使用 `CrossEntropyLoss` 计算多分类交叉熵损失，输入为模型输出的原始分类分数（logits）和真实标签，无需先手动执行 softmax。训练依次完成清空梯度、前向计算、计算 loss、反向传播和参数更新；测试时关闭梯度计算，以最高分对应的类别作为预测结果。

优化器支持 Adam、普通 SGD 和 SGD + momentum，其中 momentum 为 0.9。Adam 使用自适应更新步长，普通 SGD 根据当前梯度更新参数，带 momentum 的 SGD 累积更新方向。不同优化器需要结合各自的学习率比较。

训练及测试自动检查 MPS 是否可用：可用时使用 MPS，否则使用 CPU。单张图片预测脚本使用 CPU。

### 数据增强与评估

数据增强默认关闭，开启时仅对训练集应用 `RandomRotation(10)` 和 `RandomAffine(degrees=0, translate=(0.1, 0.1))`，分别进行最多 10 度的随机旋转和最多宽高 10% 的随机平移。测试集和预测图片不使用随机增强。

每轮记录按样本数加权的平均训练 loss 和测试 accuracy，并保存模型参数。训练曲线展示学习过程；预测与错误样本可视化帮助检查具体图片；混淆矩阵按真实类别和预测类别统计数量，对角线表示正确分类，非对角线表示误分类。

## 5. 项目文件说明

| 文件 | 作用 |
| --- | --- |
| `main.py` | 定义模型和预处理，加载数据，训练与测试，保存模型参数及每轮训练记录。 |
| `predict.py` | 加载指定模型，对单张图片进行预处理并预测数字类别。 |
| `visualize_predictions.py` | 展示测试集前 16 张图片的真实标签与预测标签，标记预测错误。 |
| `visualize_errors.py` | 遍历测试集，统计错误总数并展示前 16 个错误样本。 |
| `confusion_matrix.py` | 生成 10×10 混淆矩阵热力图，并输出最容易混淆的错误类别对。 |
| `plot_training_curve.py` | 读取训练记录 CSV，绘制平均训练 loss 和测试 accuracy 曲线。 |
| `run_experiments.py` | 自动执行四组实验，汇总最后一轮的训练损失和测试准确率。 |
| `run_repeated_experiments.py` | 对四组配置分别使用多个 seed 重复实验，保存单次结果并计算 mean/std。 |
| `experiment_notes.md` | 记录各阶段实验的目的、配置、结果、观察和结论。 |
| `experiment_results.csv` | 保存自动实验的配置及最后一轮指标。 |
| `repeated_experiment_results.csv` | 保存多 seed 重复实验的每次配置、seed 和最后一轮指标。 |
| `repeated_experiment_summary.csv` | 按配置汇总重复实验的 accuracy 与 loss 的均值和样本标准差。 |
| `README.md` | 提供环境准备、训练、预测、分析和实验脚本的使用说明。 |

## 6. 实验结果总结

以下准确率均为测试集结果。早期实验来自 `experiment_notes.md`，自动与重复实验分别来自对应 CSV。CSV 中的准确率为 0～1 小数，表中转换为百分比。不同阶段是独立实验记录，因此 Epoch 实验的 3 epochs 结果为 98.43%，后续 SimpleCNN 基线为 98.35%，两者分别保留。

### Epoch 实验

使用基础 CNN 配置，仅比较训练轮数。

| 训练轮数 | 测试准确率 |
| --- | --- |
| 1 epoch | 97.36% |
| 3 epochs | 98.43% |

增加轮数后提升 1.07 个百分点。早期记录中的 0.0676 是最后一个 batch 的 loss，不能作为整轮平均训练 loss。

### 模型结构实验

保持其他配置一致，两种模型均训练 3 epochs。

| 模型 | 训练轮数 | 测试准确率 |
| --- | --- | --- |
| SimpleCNN | 3 epochs | 98.35% |
| DeeperCNN | 3 epochs | 98.45% |

DeeperCNN 在这次比较中提升了 0.10 个百分点。

### 数据增强实验

使用 SimpleCNN，保持其他配置一致，仅改变训练集增强开关。

| 模型 | 数据增强 | 训练轮数 | 测试准确率 |
| --- | --- | --- | --- |
| SimpleCNN | 无增强 | 3 epochs | 98.35% |
| SimpleCNN | 有增强 | 3 epochs | 98.18% |

当前增强设置使准确率下降了 0.17 个百分点。

### Learning Rate 实验

使用 SimpleCNN、Adam、无增强、3 epochs、batch_size=128，仅改变学习率。

| Learning rate | 测试准确率 |
| --- | --- |
| lr=0.0005 | 98.22% |
| lr=0.001 | 98.35% |
| lr=0.005 | 98.37% |

lr=0.005 比 lr=0.001 高 0.02 个百分点，差异很小。

### Batch Size 实验

使用 SimpleCNN、Adam、无增强、3 epochs、lr=0.001，仅改变 batch size。

| Batch size | 测试准确率 |
| --- | --- |
| batch_size=64 | 98.55% |
| batch_size=128 | 98.35% |
| batch_size=256 | 98.36% |

batch_size=64 在这次比较中表现最好，比 128 高 0.20 个百分点。

### Optimizer 实验

使用 SimpleCNN、无增强、3 epochs、batch_size=64，三个优化器均使用 lr=0.001。

| 优化器 | 测试准确率 |
| --- | --- |
| Adam | 98.55% |
| SGD | 71.53% |
| SGD + momentum | 96.37% |

固定学习率时，momentum 相比普通 SGD 提升明显，但这并不能代表各优化器调参后的最佳表现。

### SGD 学习率补充实验

使用 SimpleCNN、无增强、3 epochs、batch_size=64，比较 SGD 的学习率及 momentum 设置。

| 优化器 | Learning rate | 测试准确率 |
| --- | --- | --- |
| SGD | lr=0.001 | 71.53% |
| SGD | lr=0.01 | 96.45% |
| SGD + momentum | lr=0.001 | 96.37% |
| SGD + momentum | lr=0.01 | 98.40% |

普通 SGD 提高学习率后提升了 24.92 个百分点；在 lr=0.01 时，momentum 进一步提升了 1.95 个百分点。

### 自动实验结果

四组实验均为无增强、3 epochs、batch_size=64，指标取最后一轮，来源为 [experiment_results.csv](experiment_results.csv)。

| 模型与优化器 | Learning rate | 平均训练 loss | 测试准确率 |
| --- | --- | --- | --- |
| SimpleCNN + Adam | 0.001 | 0.0424 | 98.55% |
| SimpleCNN + SGD | 0.01 | 0.1561 | 96.45% |
| SimpleCNN + SGD momentum | 0.01 | 0.0441 | 98.40% |
| DeeperCNN + Adam | 0.001 | 0.0360 | 98.92% |

本次自动实验中，DeeperCNN + Adam 的准确率最高，平均训练 loss 最低。

### 多 seed 重复实验结果

沿用自动实验的四组配置，每组使用 seed=0、1、2，共 12 次实验。结果来源为 [repeated_experiment_summary.csv](repeated_experiment_summary.csv)，各次明细见 [repeated_experiment_results.csv](repeated_experiment_results.csv)。

| 模型与优化器 | Learning rate | 测试准确率（mean ± std） |
| --- | --- | --- |
| SimpleCNN + Adam | 0.001 | 98.80% ± 0.09% |
| SimpleCNN + SGD | 0.01 | 96.56% ± 0.08% |
| SimpleCNN + SGD momentum | 0.01 | 98.82% ± 0.07% |
| DeeperCNN + Adam | 0.001 | 98.78% ± 0.14% |

mean 表示三次测试准确率的平均值，std 使用样本标准差（分母为 n−1），两者均转换为百分比并保留两位小数。例如 ±0.09% 表示准确率标准差为 0.09 个百分点，不是置信区间。

SimpleCNN + SGD momentum 的均值最高，SimpleCNN + Adam 和 DeeperCNN + Adam 与其很接近。三组均值之间仅相差 0.02～0.04 个百分点，不能据此判断某组明显更优。

## 7. 主要结论

- **增加 epoch 通常能让模型训练更充分。** 本项目从 1 epoch 到 3 epochs 的准确率由 97.36% 提升至 98.43%；更多轮数是否继续改善仍需实验验证。
- **更深模型在单次实验中有提升，但重复实验后优势不明显。** DeeperCNN + Adam 单次达到 98.92%，重复实验均值为 98.78%，与 SimpleCNN 的高准确率配置接近。
- **数据增强不一定总能提升准确率。** 当前增强使结果从 98.35% 降至 98.18%，效果取决于数据集、增强强度和训练轮数；训练更难、轮数不足是可能原因，尚需验证。
- **Learning rate 会影响训练速度和稳定性。** 它决定参数更新步长，应结合损失曲线和重复实验选择，不能仅凭微小的单次准确率差异判断。
- **batch_size=64 在本实验中表现较好。** 单次结果为 98.55%，高于 128 和 256，但不能直接推广为所有任务的最佳设置。
- **SGD 对学习率非常敏感。** lr 从 0.001 调到 0.01 后，准确率从 71.53% 提升到 96.45%，说明比较优化器时需要分别调参。
- **SGD momentum 明显优于普通 SGD。** 在两种学习率下均有明显提升，重复实验均值也由普通 SGD 的 96.56% 提升到 98.82%。
- **Adam 和 SGD momentum 都表现很好。** SimpleCNN 下重复实验均值分别为 98.80% 和 98.82%，结果接近，seed 间波动较小。
- **多 seed 重复实验比单次实验更可信。** mean/std 能同时反映平均表现和随机波动；目前每组仅三次重复，结论仍应限定在本项目配置内。

## 8. 我学到的内容

通过这个项目，我理解了 CNN 图像分类从输入图片、提取特征到输出类别的完整流程，也理解了卷积、池化和全连接层如何共同完成数字识别。

- 我学会了使用 Dataset 管理图片与标签，通过 DataLoader 实现批量加载和训练集打乱。
- 我理解了训练循环中的 `forward`、`loss`、`backward` 和 `optimizer.step()`：先得到预测，再计算误差，通过反向传播得到梯度，最后更新参数；每批训练前还要清空旧梯度。
- 我学会了保存和加载模型参数，并在加载时保持模型结构与对应实验配置一致，使用评估模式进行预测。
- 我学会了可视化预测结果和错误样本，知道整体 accuracy 无法解释每个具体错误，需要回到图片和标签检查。
- 我理解了混淆矩阵的行表示真实类别、列表示预测类别，可以用它定位容易混淆的数字类别对。
- 我学会了做控制变量实验，每次围绕一个因素比较，记录配置和结果，避免把不同设置的结果直接归因于某一个因素。
- 我认识到 mean/std 的重要性：一次最高 accuracy 可能受随机初始化和数据顺序影响，重复实验能帮助我同时评价平均效果与稳定性。

## 9. 后续改进方向

- 尝试 CIFAR-10，将学习流程扩展到更复杂的彩色图像分类任务。
- 尝试 ResNet，理解残差连接，并比较其与基础 CNN 的效果。
- 加入 TensorBoard，集中查看训练损失、准确率和不同实验的曲线。
- 使用更规范的配置文件，统一管理模型、学习率、batch size、优化器和 seed 等参数。
- 保存最佳模型，引入验证集选择最佳 checkpoint，最后使用测试集评估。
- 让多次重复实验自动生成均值、标准差和误差条图表，方便比较效果与稳定性。
